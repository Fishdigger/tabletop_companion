import React from "react"
import {Flex, Box} from "grid-styled"

export default (props) => {
    return (
        <main>
            <Flex flexWrap="wrap">
                <h1>Roll some dice</h1>
            </Flex>
        </main>
    )
}